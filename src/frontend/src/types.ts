export type Severity = 'LOW'|'MEDIUM'|'HIGH'|'CRITICAL';
export type Reason = {code?:string;message?:string;action?:string;[key:string]:unknown};
export type Port = {id:string;name:string;latitude:number;longitude:number};
export type Terminal = {id:string;name:string;port_id:string;yard_capacity_teu:number};
export type Berth = {id:string;name:string;terminal_id:string;length_m:number;depth_m:number;max_cranes:number};
export type Crane = {id:string;berth_id:string;productivity_moves_per_hour:number};
export type Vessel = {id:string;name:string;length_m:number;draft_m:number;capacity_teu:number};
export type Call = {id:string;vessel_id:string;terminal_id:string;scheduled_eta:string;priority:number;cargo_type:string;unload_moves:number;load_moves:number;teu_per_move:number};
export type ForecastSource = {kind:string;label:string;source_type:'OBSERVED'|'SCHEDULED'|'FORECAST_ASSUMPTION'|'SCENARIO_OVERRIDE';source_timestamp:string|null;effective_timestamp:string|null;start:string|null;end:string|null;age_hours:number|null;freshness:'FRESH'|'STALE'|'NOT_APPLICABLE'|'UNKNOWN';values:Record<string,unknown>;assumptions:string[]};
export type ForecastExplanation = {forecast_run_id:string;input_hash:string;projection_method:string|null;severity_explanation:string;triggered_thresholds:{code:string;metric:string;value:number;threshold:number;unit:string;alert_enabled:boolean|null;explanation:string}[];confidence_explanation:string;inputs:ForecastSource[];operator_interpretation:string;supervisor_action:'Monitor'|'Review before next shift'|'Run rolling replan'|'Escalate for approval';action_reason:string;human_approval_required:boolean;plan_status:string|null;assumptions:string[];missing_provenance:string[]};
export type Forecast = {id:string;run_id?:string;scope:'port'|'terminal'|'berth';scope_id:string;port_id:string;terminal_id:string|null;berth_id:string|null;timestamp:string;berth_utilisation:number;queue_length:number;average_wait_hours:number;yard_occupancy:number;crane_utilisation:number;congestion_probability:number;probability_lower?:number;probability_upper?:number;probability_basis?:'trained_scope_model'|'terminal_model_prior';prediction_timestamp?:string;explanation?:ForecastExplanation|null;congestion_severity:Severity;confidence_level:string;confidence_reasons:string[];main_causes:Reason[];is_hotspot:number|boolean;first_expected_hotspot_time:string|null;expected_hotspot_duration_hours:number;model_version:string};
export type Prediction = {call_id:string;port_id:string;terminal_id:string;prediction:number;lower:number;upper:number;factors:Reason[];model_version:string};
export type Segment = {start_slot:number;end_slot:number;crane_ids:string[];moves:number};
export type Assignment = {call_id:string;berth_id:string;start:string;end:string;completion_time:string;waiting_minutes:number;planned_moves:number;execution_profile:{origin:string;segments:Segment[];shift_cranes:number[]};cranes?:{crane_id:string}[]};
export type Task = {call_id:string;berth_id:string;crane_ids:string[];start:string;end:string;planned_moves:number;task_type:string};
export type Shift = {shift_index:number;start:string;end:string;tasks:Task[];details:{incoming:Reason[];waiting:Reason[];berthing:Reason[];departing:Reason[];berth_assignments:Reason[];crane_allocation:Reason[];planned_container_moves:number;projected_yard_occupancy:Reason[];weather_constraints:Reason[];tide_constraints:Reason[];maintenance_and_equipment_restrictions:Reason[];high_risk_handoffs:Reason[];congestion_alerts:Reason[];required_supervisor_actions:Reason[];approved_diversions_or_arrival_changes:Reason[];contingency_actions:Reason[];handover_notes:string[];confidence:string}};
export type Plan = {id:string;status:'DRAFT'|'REVIEWED'|'APPROVED'|'SUPERSEDED';revision:number;shifts:Shift[];document:{confidence:string;changes_compared_with_approved_plan:Reason[];unresolved_conflicts:Reason[];assumptions:string[];model_version:string;operational_changes:Reason[]};previous_plan_id:string|null};
export type Metrics = {validation_passed?:boolean;average_wait_hours:number;maximum_wait_hours:number;berth_utilisation:number;crane_utilisation:number;delayed_vessels:number;estimated_cost_usd:number;estimated_emissions_tonnes_co2:number;deferred_vessels:number;served_vessels:number;demand_average_wait_proxy_hours:number;yard_peak_occupancy:number};
export type Run = {id:string;scenario_id:string|null;as_of:string;end:string;status:string;solver_status:string;solver_runtime_ms:number;assignments:Assignment[];plan:Plan|null;metrics:Metrics;comparison:{baseline:Metrics;optimised:Metrics;baseline_assignments:Assignment[];same_served_vessels:boolean;estimated_cost_savings_usd:number;estimated_emissions_savings_tonnes_co2:number};diagnostics:{unscheduled_call_ids:string[];infeasibility_explanations:Reason[]}};
export type Recommendation = {id:string;run_id?:string;call_id:string;port_id:string;recommended_action:string;expected_hours_saved:number|null;estimated_cost_change_usd:number|null;estimated_emissions_change_tonnes:number|null;confidence_level:string;is_expired:boolean;expires_at:string;main_reasons:Reason[];risks_and_assumptions:string[];current_plan_outcome:Outcome|null;recommended_plan_outcome:Outcome|null;options:{action:string;eligible:boolean;rejection_codes:string[];expected_hours_saved:number|null}[]};
export type Outcome = {terminal_id:string;planned_wait_hours:number;expected_delivery:string;berth_start:string;total_cost_usd:number;total_co2_tonnes:number};
export type Dashboard = {risk_wait_threshold_hours:number;effective_assignments:Assignment[];run:Run;inventory:{routing_ports:Port[];terminals:Terminal[];berths:Berth[];cranes:Crane[];vessels:Vessel[];calls:Call[];yards:Reason[];weather:Reason[];availability:{crane_id:string;start:string;end:string;reason:string}[];disruptions:Reason[]};forecast_rows:Forecast[];waiting_predictions:Prediction[];choices:{id:string;name:string;scenario:boolean;as_of:string;plan_status:string|null}[];recommendations:Recommendation[];routes:{call_id:string;port_id:string;coordinates:[number,number][];basis:string;remaining_distance_nm:number}[];alerts:(Reason&{id:string;state:string;revision:number;rule_code:string;expected_start:string})[];notices:string[];scenario_name:string};
